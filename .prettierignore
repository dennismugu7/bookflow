node_modules/
dist/
build/
coverage/
package-lock.json

# Generated output (ADR-025). Regenerated, never hand-edited, and excluded from
# format rules for the reason ADR-025 gives.
packages/contracts/
apps/api/src/platform/db.types.ts

# Scratch space written by `supabase start`. Gitignored by supabase/.gitignore;
# Prettier does not read that.
supabase/.temp/

# The Flutter tree has its own toolchain (ADR-022): `dart format` owns the Dart
# sources, and the JSON and YAML under it are generated by `flutter create` and
# the platform build systems. Prettier reformatting either would put the two
# formatters in a fight neither can win.
apps/mobile/

# Prose. Every markdown file in this repository is hand-authored with
# deliberate line breaks and hand-aligned tables; Prettier reflows both.
# `docs/source/` is immutable besides, so formatting it is not an option.
*.md
