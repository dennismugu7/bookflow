{
  "x-generated": "DO NOT EDIT THIS FILE BY HAND. Generated from the Zod route schemas in apps/api by `npm run contracts:generate`. ADR-014: the spec is generated from code, never hand-written. ADR-025: CI regenerates it and fails on any diff, so a hand edit is a build failure.",
  "openapi": "3.1.0",
  "info": {
    "title": "Bookflow API",
    "version": "0.0.0",
    "description": "Generated from the Zod route schemas in apps/api. Never hand-written (ADR-014, ADR-025)."
  },
  "components": {
    "schemas": {
      "HealthResponseInput": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "const": "ok"
          }
        },
        "required": [
          "status"
        ],
        "description": "Liveness response."
      },
      "HealthResponse": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "const": "ok"
          }
        },
        "required": [
          "status"
        ],
        "additionalProperties": false,
        "description": "Liveness response."
      }
    }
  },
  "paths": {
    "/health": {
      "get": {
        "operationId": "getHealth",
        "summary": "Liveness probe",
        "tags": [
          "health"
        ],
        "description": "Reports that the process is up and answering. Deliberately says nothing about the environment, the version or the database — this endpoint is unauthenticated.",
        "responses": {
          "200": {
            "description": "Default Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            }
          }
        }
      }
    }
  }
}
